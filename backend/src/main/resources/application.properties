spring:
application:
name: bookingshow-intern

datasource:
url: jdbc:mysql://localhost:3306/bookingshow_db?useSSL=false&serverTimezone=UTC
username: root
password: your_password

jpa:
hibernate:
ddl-auto: update          # Tự tạo bảng khi chạy
show-sql: true
properties:
hibernate:
dialect: org.hibernate.dialect.MySQLDialect

server:
port: 8080

# Swagger
springdoc:
swagger-ui:
path: /swagger-ui.html